# 安全策略

ANL MineOps 当前是本地自托管的 Experimental/Beta 项目，不适合直接暴露到公网或用于无人值守的生产恢复操作。

## 报告安全问题

请不要在公开 Issue 中提交可利用细节、API Key、RCON 密码、Cookie、服务器凭据、世界文件、备份或完整模组源码。发布仓库后，请优先使用 GitHub Private Security Advisory；如果仓库尚未启用该功能，请在 Issue 中只留下不敏感的联系方式和简短影响范围。

报告应包含：受影响版本、操作系统、适配器或模组项目类型、复现步骤、影响、临时缓解方式和脱敏日志。

## 当前安全边界

- 默认只绑定 `127.0.0.1`；非本机访问必须显式启用 Bearer token。
- 模型不能直接执行任意 shell 或任意 RCON。
- 模组工作区拒绝路径穿越、敏感文件和符号链接逃逸。
- 补丁应用、构建、服务器生命周期操作和恢复按工具策略执行；高风险动作需要审批或默认禁用。
- API Key 只通过环境变量引用，不写入配置、审计摘要或 README。
- Demo 测试不启动真实 Minecraft，不上传本地文件。
