# ANL MineOps：面向 Minecraft 服主的 Agent

## Summary

- 之前选择 `OpenClaw` 是抽象层级判断错误：它已经包含网关、会话、记忆、技能、渠道和 Agent runtime，适合作为后续 QQ/Discord 通道适配器，不适合作为你学习 Agent 核心的底座。
- 首发采用：`Python + PydanticAI + FastAPI + React/Vite/TypeScript + SQLite`。
- 目标不是重写所有协议，而是自己掌握 Agent 的工具、权限、状态、provider 和业务逻辑。
- 首发定位为“安全运维 Beta”：状态、日志、崩溃诊断、启动/停止/重启、备份/校验/恢复、审批和审计。
- 在安全运维 Beta 内增加一个 Experimental 模组开发与验证工作台，参考 ModCrafting 的“计划 → 工具执行 → 证据”闭环，但保持 MineOps 自己的权限、审批、审计和本地自托管边界。
- 首发适配 `Bedrock/BDS + Endstone`，本地自托管；不把真实 BDS 启动作为发布门槛。

调研依据：

- [MineBBS 开服生态讨论](https://www.minebbs.com/threads/mc.45281/)：低基础门槛、服务器同质化、获客和留存困难。
- [MineBBS 全岗位招募](https://www.minebbs.com/threads/craft.46121/)：运维、备份、部署、BUG 处理、玩法、客服和宣传都是实际岗位。
- [MineBBS 服务器崩溃案例](https://www.minebbs.com/threads/37160/)：区块损坏、硬盘风险和异地备份问题。
- [Endstone](https://github.com/EndstoneMC/endstone)：BDS、Python/C++ 插件、Windows/Linux 和 Paper-like API。
- [Minecraft RCON MCP Server](https://github.com/MinecraftCodeFoundary/Minecraft-MCP-Server)、[ReyxGPT](https://github.com/chrismethsillo/reyxgpt)：现有项目主要是 RCON 命令和状态查询，说明不能只做一个自然语言 RCON 外壳。
- [ModCrafting](https://github.com/newstarbar/ModCrafting)：参考其自然语言需求、Plan/Execute Agent、受限工具调用、独立构建和测试场景思路；不复制其源码，不把真实 Minecraft 客户端作为首发 CI 门槛。
- [PydanticAI](https://pydantic.dev/docs/ai/overview/)、[LangGraph](https://docs.langchain.com/oss/python/langgraph/overview)、[MCP 架构](https://modelcontextprotocol.io/docs/learn/architecture)：分别对应 Agent 工具层、长流程编排层和工具协议层。

## Implementation

### Agent 与模型层

- 用 PydanticAI 定义带类型的 `Agent`、`RuntimeDeps`、工具参数和 `AgentAnswer`。
- 使用 `agent.iter()` / 事件流理解和展示每一步，不添加隐藏的“修复 Agent”或无提示的二次调用。
- 保留一个很小的教学实验模块，展示消息、工具调用、工具结果和停止条件；它只用于学习，不进入生产路径。
- 第一版 provider 接口支持：
  - 自定义 `base_url`
  - 自定义模型名
  - `api_key_env` 环境变量引用
  - 流式输出和工具调用能力探测
  - OpenAI-compatible Chat/Responses 作为首发协议
- 后续再增加 Anthropic Messages、Gemini 原生协议；不承诺一次覆盖所有供应商。
- `anlapi` 作为可选 provider 预设，配置页和 README 展示 `https://api.anlmc.top`、QQ群 `146499741`，但不强制路由、不在每条回复插广告、不把密钥写入配置或日志。

### Minecraft 领域层

定义稳定的 `ServerAdapter` 接口，Agent 不直接执行任意 shell：

- `get_status`
- `get_logs`
- `get_players`
- `start`
- `stop`
- `restart`
- `create_backup`
- `verify_backup`
- `restore_backup`

首发实现：

- `DemoAdapter`：无真实服务器即可演示和测试。
- `BdsProcessAdapter`：本地 BDS 进程、stdin、日志目录和文件系统。
- `EndstoneRconAdapter`：可选连接 Endstone RCON 插件；Endstone 没有内置 RCON，因此作为独立能力安装。
- 备份优先使用 save-safe 流程；若服务器能力不足，则停止或锁定后备份，并明确提示风险。
- 不提供默认开放的“执行任意控制台命令”工具，只允许显式配置的命令白名单。

### ModCrafting 参考功能：模组开发与验证工作台（Experimental）

目标是把“让 Agent 帮服主改模组”拆成可观察、可审批、可回滚的步骤，而不是把模型变成任意 shell 执行器。

已完成：

- `ModTaskPlan`：把自然语言模组需求转换为结构化目标、假设、步骤和下一动作。
- `ModWorkspaceService`：限制项目根目录，只允许安全的相对路径；拒绝路径穿越、敏感文件、符号链接逃逸和二进制文本读取。
- 项目分析和文件读取：记录文件大小、语言、SHA-256、loader 特征和风险提示。
- `ModPatch`：生成带 `expected_sha256` 的补丁草稿；应用前重新校验，原子写入失败时回滚。
- `build_mod_project`：只运行用户显式配置的参数数组；拒绝 shell 操作符、`-c`/`/c` 等任意代码入口；默认需要审批。
- 六类场景模板：`new_item`、`new_block`、`new_recipe`、`entity_behavior`、`player_interaction`、`hud_gui`。
- `DemoModTestAdapter`：使用版本库中的固定 fixture 输出结构化测试证据，不启动真实 Minecraft 客户端，不伪造截图或游戏日志。
- Agent typed tools、SSE、MCP、SQLite、前端“模组工作台”复用同一套权限和审计路径。

后续实现：

- 接入真实 Minecraft Bridge，提供独立客户端启动、游戏状态观察、截图和崩溃报告回传。
- 增加可视化 diff、资源引用检查、构建产物下载和更细的 Gradle 沙箱。
- 真实客户端验证成熟后，再考虑把 Forge/NeoForge、基岩版 Add-on 和 Endstone 插件测试适配器分别接入。

### 权限、审批与记忆

每个工具都有代码级元数据：

```text
risk: read | mutate | destructive
mode: auto | confirm | disabled
timeout
allowed_server_ids
```

- 用户可以逐工具设置自动执行、确认后执行或禁用。
- 初始模板默认：读操作自动，普通写操作确认，恢复/删除等破坏性操作禁用；用户可自行调整。
- 审批记录包含过期时间、一次性执行标识和操作者。
- 所有工具调用、参数摘要、结果摘要、错误和审批都写入 SQLite 审计表。
- 会话记忆与服务器事实分离；模型自己说的话不能自动写入长期记忆，只有用户明确保存的服务器资料才进入持久化。

### Web 与公开 API

后端：

- FastAPI
- Pydantic v2 schema
- SSE 推送 Agent 事件
- 本地单用户模式，默认绑定 `127.0.0.1`
- 非本机访问需要显式启用 Bearer token

前端：

- React + Vite + TypeScript
- TanStack Query 管理 API 状态
- EventSource 展示实时 Agent/tool 事件
- 页面包括：仪表盘、对话、审批队列、服务器详情、日志、备份、provider 配置、工具权限、审计记录。

公开接口统一使用 `/api/v1` 和 `{data, meta, error}` 响应结构，至少包含：

- `POST /api/v1/runs`
- `GET /api/v1/runs/{id}`
- `GET /api/v1/runs/{id}/events`
- `GET/POST/PATCH /api/v1/providers`
- `GET/PATCH /api/v1/tool-policies`
- `GET/POST /api/v1/approvals`
- `GET /api/v1/servers`
- `POST /api/v1/servers/{id}/actions/{action}`
- `GET /api/v1/audit-events`
- `GET/POST /api/v1/mod-projects`
- `GET/POST /api/v1/mod-projects/{id}/plans`
- `GET /api/v1/mod-projects/{id}`、`GET /api/v1/mod-projects/{id}/files/{path}`
- `GET/POST /api/v1/mod-projects/{id}/patches`、`POST .../patches/{patch_id}/apply`
- `GET/POST /api/v1/mod-projects/{id}/builds`
- `GET/POST /api/v1/mod-projects/{id}/scenarios`、`POST .../scenarios/{scenario_id}/run`
- `GET /api/v1/mod-projects/{id}/evidence`

MCP 使用官方 Python SDK，把同一套领域工具暴露给 Claude、Cursor 等客户端；MCP 层不能绕过现有权限和审计服务。LangGraph 暂缓，只有在需要跨重启恢复的长流程更新、迁移或灾备任务时再引入。

## 测试与发布

- 不运行真实本地服务器作为发布条件。
- 使用 `DemoAdapter`、固定日志、崩溃报告和备份文件 fixture 完成测试。
- PydanticAI 使用 `TestModel`，全局禁止测试时真实模型请求。
- 测试场景：
  - 根据日志诊断崩溃并引用证据。
  - 重启请求进入审批，批准后才执行并产生审计记录。
  - 备份前检查磁盘空间、生成校验值、失败时保留原状态。
  - 恢复操作默认被阻止，启用后需要二次确认。
  - 自定义 provider 只保存环境变量名，不泄漏 API Key。
  - provider 不支持工具调用时，Agent 明确降级为只读，不伪造执行结果。
  - 恶意日志内容不能改变系统权限。
- 模组项目分析只返回项目根内文件，不能通过路径穿越读取外部文件。
- 补丁文件被外部修改后，`expected_sha256` 校验失败且不覆盖原文件。
- 补丁应用、构建和恢复操作按策略进入审批，审批批准后才执行并产生审计记录。
- 六类 Demo 测试场景输出步骤、断言、匹配文件和证据 ID；不声称真实客户端已经运行。
- 自定义构建命令只接受显式参数数组，不执行任意 shell 字符串。
- GitHub Actions 执行 Python 测试、类型检查、前端构建、SSE/API 测试、密钥扫描和 UTF-8 检查。
- Beta 发布资料已准备：README 公测流程、GitHub Beta 发布说明、MineBBS 招募帖和 GitHub Bug/功能 Issue 模板；GitHub 公开仓库已创建并推送，地址为 `https://github.com/ANL-694/anl-mineops`。
- 以 MIT 开源，标记为 Experimental/Beta；README 提供安装、配置、隐私说明、已知限制和反馈模板。
- MineBBS 发布招募帖，说明“不要求测试者提供密钥、不上传日志、由测试者自行连接自己的服务器”，并收集版本、适配器、错误日志和复现步骤。
- GitHub/MineBBS 公测材料补充模组工作台说明：测试者自行提供项目目录，不上传源码、密钥或日志；反馈需包含 loader、Minecraft 版本、构建命令和脱敏错误。
- 加入 Minecraft 非官方项目声明，避免暗示 Mojang/Microsoft 背书。
- v2 再做玩家留存分析、活动/任务策划、宣传文案和 QQ/Discord 通道。

## Assumptions

- 暂定项目名为 `ANL MineOps`，首发语言和界面以中文为主。
- v1 面向单个服主或小团队，不做集中式 SaaS、多租户计费和云端托管。
- `anlapi` 推广仅出现在 provider 预设、首次配置页、README 和 MineBBS 介绍中，明确标注为作者推荐并允许隐藏。
- 当前代码已完成安全运维基础能力和 ModCrafting 参考工作台 MVP，并已提交、推送 GitHub；尚未发布 MineBBS。真实 Minecraft Bridge、截图观测和外部公测仍属于后续阶段。
